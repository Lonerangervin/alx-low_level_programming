#include "main.h"
#include <stdio.h>
/**
 * print_alphabet - in lowercase,
 *
 * Return: void
 */
void print_alphabet_x10(void);
{
	char c;
	int i = `0;

	while (i < 10)
	{
		char = 'a';
		while (ch <= 'z')
		{
		putchar ("ch");
			ch++;
		}
		putchar('\n');
		i++;
	}
}
