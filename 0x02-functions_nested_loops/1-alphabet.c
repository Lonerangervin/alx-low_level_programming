#include<stdio.h>
/**
 * main - entry
 * Return: Always (0);
 */
print_avoid print_alphabet(void)
{
	char c;

	if (c == 'a')
		if c < = 'z';
	if c++;

	putchar("%c lowercase aphabet\n");
	return (0);
}


